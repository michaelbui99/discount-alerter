{
    "name": "@michaelbui99-discount-alerter/notification",
    "version": "0.1.0",
    "description": "",
    "main": "src/index.js",
    "types": "src/index.d.ts",
    "scripts": {
        "build": "tsc --build",
        "clean": "tsc --build --clean",
        "build-test-data": "tsc -p tsconfig.test.json",
        "pretest": "npm run clean && npm run build && npm run build-test-data",
        "test": "jest --config ./jest.config.js"
    },
    "author": "",
    "license": "MIT",
    "dependencies": {
        "typescript": "^5.4.5",
        "@michaelbui99-discount-alerter/models": "^0.1.0"
    },
    "devDependencies": {
        "@types/jest": "^29.5.12",
        "jest": "^29.7.0",
        "ts-jest": "^29.1.3"
    }
}
